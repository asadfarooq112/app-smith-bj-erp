SELECT
  COALESCE(SUM(amount), 0) AS total_refunds
FROM
  bj.financial_transactions
WHERE
  type = 'refund'
  AND date_added >= (CURRENT_DATE AT TIME ZONE 'Asia/Karachi')
  AND date_added < ((CURRENT_DATE + INTERVAL '1 day') AT TIME ZONE 'Asia/Karachi');
