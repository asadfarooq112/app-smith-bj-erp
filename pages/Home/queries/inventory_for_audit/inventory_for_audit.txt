SELECT
  i.code,
  s.image,
  i.metal_weight,
  i.diamond_weight,
  i.size
FROM
  bj.inventory AS i
JOIN
  bj.sku AS s
  ON s.code = i.code
WHERE
  i.branch_id = 'a758efa5-39c1-4a52-a27b-55b8cc979487'::uuid
  AND i.is_sold = false
ORDER BY
  s.category,
  i.code,
  i.size;
