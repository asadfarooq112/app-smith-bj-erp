SELECT
  *
FROM
  bj.sku
WHERE
  "code" ilike '%{{Table1.searchText}}%'
ORDER BY
  "{{Table1.sortOrder.column || 'sku_id'}}" {{Table1.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table1.pageSize}}
OFFSET
  {{Table1.pageOffset}}